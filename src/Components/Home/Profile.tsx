import React from 'react'

export type ProfileProps={
    name: string
}

export default function Profile({name}:ProfileProps) {
  return (
    <div>
      profile details and fantastic and logical images verified.
    </div>
  )
}
